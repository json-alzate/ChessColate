import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Positions960Page } from './positions960.page';

describe('Positions960Page', () => {
  let component: Positions960Page;
  let fixture: ComponentFixture<Positions960Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Positions960Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
